# 使用官方 Python 基础镜像
FROM python:3.12

# 设置工作目录
WORKDIR /test

# 复制依赖文件并安装
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制项目文件
COPY . .

# 启动应用
CMD ["pytest", "test_api.py", "-v"]